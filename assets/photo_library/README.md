Drop your Singapore/campaign photo library here (skyline shots, Merlion, Marina Bay
Sands, students, professionals, etc). Reference them by filename in a poster config's
`photos.hero` / `photos.secondary` fields, then run generate_poster.py with
`--photos-dir` pointing at this folder (or wherever the photos actually live).
